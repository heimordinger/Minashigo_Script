# 识图目录（辅助识图自动生成，可手改；图片文件变更后会重新识图）
# prompt_version=1
#
# 尺度：运行时自动定标并按帧尺寸缓存；窗口尺寸变化会重校。
# 可选：同目录放 match_ref.json（ref_width/ref_height）可跳过定标（非必须）。

挑战.png：按钮；界面「挑战界面」；挑战按钮，点击之后进入战斗。也可作为挑战界面标识

金币.png：可作为「战斗结算」的标识图；结算标识

结界/勋章.png：结界突破对手卡上的实心勋章；用于九宫格多目标计数（见 scripts/yys/结界勋章.py）
